@model LMSBL.DBModels.TblTenant
@{
    ViewBag.Title = "Delete Tenant";
    //Layout = "~/Views/_ViewStart.cshtml";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DeleteTenant</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Delete Tenant</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.TenantName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TenantName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.TenantDomain)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TenantDomain)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ActivationFrom)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ActivationFrom)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ActivationTo)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ActivationTo)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.NoOfUserAllowed)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NoOfUserAllowed)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            @if (ViewBag.isActive == true)
            {
                <input type="submit" value="Deactivate" class="btn btn-default btn-success" />

            }
            else
            {
                <input type="submit" value="Activate" class="btn btn-default btn-success" />
            }
            @Html.ActionLink("Back to List", "Index")
        </div>
        }
    </div>
</body>
</html>
